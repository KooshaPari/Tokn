{
  "name": "tokenledger-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:index": "bash scripts/generate-doc-index.sh",
    "docs:dev": "vitepress dev .",
    "docs:build": "bash scripts/generate-doc-index.sh && vitepress build .",
    "docs:preview": "vitepress preview ."
  },
  "devDependencies": {
    "vitepress": "^1.6.0"
  },
  "overrides": {
    "esbuild": "^0.25.0",
    "vite": "^6.4.2"
  }
}
