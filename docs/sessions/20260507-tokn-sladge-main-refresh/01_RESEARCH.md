# Research

## Local State

- Canonical Tokn was clean on `main` at `1c34d27`.
- The branch is locally `ahead 7, behind 1` relative to `origin/main`.
- Existing prepared branch `docs/tokn-sladge-current` was not a fast-forward
  from current local head: `git rev-list --left-right --count` reported `6 2`.
- Repository instructions say feature branches use worktrees and to never
  commit directly to `main`.

## Sladge Target Rationale

The README describes enterprise-grade token management and pricing governance
for AI coding agents, with cost tracking across multiple AI provider APIs and
optimization recommendations, so the badge is appropriate disclosure.
