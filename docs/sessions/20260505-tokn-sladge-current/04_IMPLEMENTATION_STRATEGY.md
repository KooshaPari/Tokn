# Implementation Strategy

Use a current-head feature worktree to replace stale prepared badge evidence.
Keep the implementation narrow: badge line in the existing README badge block
plus session documentation. Avoid canonical integration in this step because
Tokn branch discipline says not to commit directly to `main`.
